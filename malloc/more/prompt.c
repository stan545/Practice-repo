#include <stdio.h>
#include <stdlib.h>
#nclude "main.h"

ssize_t _getline(char **lineptr, size_t *n, FILE *stream)
{
	char  *bufRead;
	int arr;
	int index = 0;

	lineptr = malloc(sizeof(bufRead));
	n = &index;
	while (bufRead[index])
	{
		index++;
	}

	return (counter);
}
